
name: scGPT_env
channels:
  - conda-forge
  - defaults
dependencies:
  - ipykernel
  - pip
  - pip:
    - anndata
    - scanpy
    - numpy
    - pandas
    - dask[dataframe]
    - scipy
    - tqdm
    # If scgpt is not a publicly available package, it would be marked or installed via a direct pip install command after environment creation.
    # - scgpt # mark if not available
